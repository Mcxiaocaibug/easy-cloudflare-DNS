# ===========================================
# Cloudflare DNS 管理系统 - 环境变量配置
# ===========================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据您的需求修改配置
# 3. 运行 docker-compose up -d 启动服务

# ===========================================
# 数据库配置（支持两种方式）
# ===========================================
# 方式1: 使用 MySQL DSN（推荐）
# 格式: mysql://username:password@host:port/database
# 示例: mysql://cloudflare:cloudflare_password_123@mysql:3306/cloudflare_dns
DATABASE_URL=mysql://cloudflare:cloudflare_password_123@mysql:3306/cloudflare_dns

# 方式2: 使用分离的环境变量（如果未设置 DATABASE_URL，则使用以下配置）
# 数据库类型（mysql 或 external_mysql）
DB_TYPE=mysql

# MySQL数据库主机地址（内部使用 'mysql'，外部使用实际IP/域名）
DB_HOST=mysql

# MySQL数据库端口
DB_PORT=3306

# 数据库名称
DB_NAME=cloudflare_dns

# 数据库用户名
DB_USER=cloudflare

# 数据库密码
DB_PASSWORD=cloudflare_password_123

# 数据库root密码（仅用于内置MySQL容器）
DB_ROOT_PASSWORD=root_password_123

# 数据库字符集
DB_CHARSET=utf8mb4

# ===========================================
# 管理员账户配置（首次安装时创建）
# ===========================================
# 管理员用户名
ADMIN_USERNAME=admin

# 管理员密码（建议设置复杂密码）
ADMIN_PASSWORD=admin123456

# 管理员邮箱（可选，用于密码重置）
ADMIN_EMAIL=admin@example.com

# ===========================================
# 系统基础配置
# ===========================================
# 网站名称
SITE_NAME=Cloudflare DNS管理系统

# 每条DNS记录消耗积分
POINTS_PER_RECORD=1

# 新用户默认积分
DEFAULT_USER_POINTS=100

# 是否允许用户注册（1=允许，0=禁止）
ALLOW_REGISTRATION=1

# ===========================================
# 邀请系统配置
# ===========================================
# 是否启用邀请系统（1=启用，0=禁用）
INVITATION_ENABLED=1

# 邀请成功奖励积分
INVITATION_REWARD_POINTS=10

# 被邀请用户额外积分
INVITEE_BONUS_POINTS=5

# ===========================================
# SMTP邮件配置
# ===========================================
# 是否启用SMTP邮件发送（1=启用，0=禁用）
SMTP_ENABLED=0

# SMTP服务器地址（如：smtp.qq.com）
SMTP_HOST=smtp.example.com

# SMTP服务器端口（通常：465用于SSL，587用于TLS）
SMTP_PORT=465

# SMTP用户名（通常是发件邮箱）
SMTP_USERNAME=

# SMTP密码（通常是授权码）
SMTP_PASSWORD=

# SMTP加密方式（ssl 或 tls）
SMTP_SECURE=ssl

# 发件人显示名称
SMTP_FROM_NAME=六趣DNS

# SMTP调试模式（0=关闭，1-4=不同级别的调试信息）
SMTP_DEBUG=0

# ===========================================
# 应用配置
# ===========================================
# 应用对外端口
APP_PORT=8080

# 是否自动安装（1=启用，0=禁用，首次部署建议启用）
AUTO_INSTALL=1

# ===========================================
# 外部数据库配置示例
# ===========================================
# 方式1: 使用 DATABASE_URL（推荐）
# DATABASE_URL=mysql://your_user:your_password@your-host.com:3306/cloudflare_dns

# 方式2: 使用分离变量
# DB_TYPE=external_mysql
# DB_HOST=your-external-mysql-host.com
# DB_PORT=3306
# DB_NAME=cloudflare_dns
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password

# 更多配置示例，请参考: DATABASE_DSN_GUIDE.md

# ===========================================
# 常见SMTP配置示例
# ===========================================
# QQ邮箱示例：
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=465
# SMTP_USERNAME=your_email@qq.com
# SMTP_PASSWORD=your_authorization_code
# SMTP_SECURE=ssl

# Gmail示例：
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_SECURE=ssl

# 163邮箱示例：
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465
# SMTP_USERNAME=your_email@163.com
# SMTP_PASSWORD=your_authorization_code
# SMTP_SECURE=ssl

